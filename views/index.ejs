<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - <%= siteName %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/header') %>

    <!-- Hero Section -->
    <section class="hero bg-primary text-white py-5">
        <div class="container py-5">
            <div class="row align-items-center">
                <div class="col-lg-6">
                    <h1 class="display-4 fw-bold mb-4">Bienvenue à l'ASC Zone de Tir</h1>
                    <p class="lead mb-4">
                        Une association dédiée au sport, à la culture et à la convivialité.
                        Rejoignez-nous pour vivre une expérience unique au Sénégal !
                    </p>
                    <div class="d-flex gap-3">
                        <a href="/auth/inscription" class="btn btn-light btn-lg">
                            <i class="bi bi-person-plus me-2"></i>Nous rejoindre
                        </a>
                        <a href="/qui-sommes-nous" class="btn btn-outline-light btn-lg">
                            En savoir plus
                        </a>
                    </div>
                </div>
                <div class="col-lg-6 mt-4 mt-lg-0">
                    <img src="/images/hero-image.jpg" alt="ASC Zone de Tir" class="img-fluid rounded shadow"
                         onerror="this.src='https://placehold.co/600x400/1e40af/white?text=ASC+Zone+de+Tir'">
                </div>
            </div>
        </div>
    </section>

    <!-- Slider des Activités Sportives -->
    <section class="activities-slider py-5 bg-light">
        <div class="container">
            <div class="row mb-4">
                <div class="col-lg-12 text-center">
                    <h2 class="h3 fw-bold mb-3">Nos Activités Sportives</h2>
                    <p class="text-muted">Découvrez nos différentes disciplines sportives</p>
                </div>
            </div>
            <% if (activities && activities.length > 0) { %>
                <div id="activitiesCarousel" class="carousel slide" data-bs-ride="carousel">
                    <div class="carousel-indicators">
                        <% activities.forEach((activity, index) => { %>
                            <button type="button" data-bs-target="#activitiesCarousel" data-bs-slide-to="<%= index %>"
                                    class="<%= index === 0 ? 'active' : '' %>"
                                    aria-current="<%= index === 0 ? 'true' : 'false' %>"
                                    aria-label="Slide <%= index + 1 %>"></button>
                        <% }); %>
                    </div>
                    <div class="carousel-inner">
                        <% activities.forEach((activity, index) => { %>
                            <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                                <div class="row align-items-center">
                                    <div class="col-md-6">
                                        <img src="<%= activity.image %>" class="d-block w-100 rounded shadow-lg activity-image"
                                             alt="<%= activity.title %>"
                                             onerror="this.src='https://placehold.co/800x600/1e40af/white?text=<%= encodeURIComponent(activity.title) %>'">
                                    </div>
                                    <div class="col-md-6 px-5">
                                        <div class="activity-content">
                                            <div class="mb-3">
                                                <i class="<%= activity.icon %> text-primary" style="font-size: 3rem;"></i>
                                            </div>
                                            <h3 class="fw-bold mb-3"><%= activity.title %></h3>
                                            <p class="lead text-muted"><%= activity.description %></p>
                                            <span class="badge bg-primary mb-3">
                                                <%= activity.category.charAt(0).toUpperCase() + activity.category.slice(1) %>
                                            </span>
                                            <div class="mt-4">
                                                <a href="/activites" class="btn btn-primary">
                                                    <i class="bi bi-info-circle me-2"></i>En savoir plus
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                    <button class="carousel-control-prev" type="button" data-bs-target="#activitiesCarousel" data-bs-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Précédent</span>
                    </button>
                    <button class="carousel-control-next" type="button" data-bs-target="#activitiesCarousel" data-bs-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Suivant</span>
                    </button>
                </div>
            <% } else { %>
                <div class="col-12">
                    <p class="text-muted text-center">Aucune activité disponible pour le moment.</p>
                </div>
            <% } %>
        </div>
    </section>

    <!-- Actualités -->
    <section class="py-5">
        <div class="container">
            <div class="row mb-4">
                <div class="col-lg-12">
                    <h2 class="h3 fw-bold mb-3">Dernières actualités</h2>
                </div>
            </div>
            <div class="row g-4">
                <% if (articles && articles.length > 0) { %>
                    <% articles.slice(0, 3).forEach(article => { %>
                        <div class="col-md-6 col-lg-4">
                            <div class="card h-100 shadow-sm">
                                <% if (article.featuredImage) { %>
                                    <img src="<%= article.featuredImage %>" class="card-img-top" alt="<%= article.title %>"
                                         onerror="this.src='https://placehold.co/400x250/f0f0f0/333?text=Image'">
                                <% } else { %>
                                    <img src="https://placehold.co/400x250/f0f0f0/333?text=<%= article.title %>" class="card-img-top" alt="<%= article.title %>">
                                <% } %>
                                <div class="card-body">
                                    <span class="badge bg-primary mb-2"><%= article.category %></span>
                                    <h5 class="card-title"><%= article.title %></h5>
                                    <p class="card-text text-muted small"><%= article.excerpt %></p>
                                    <div class="d-flex justify-content-between align-items-center">
                                        <small class="text-muted">
                                            <i class="bi bi-calendar me-1"></i>
                                            <%= new Date(article.publishedAt).toLocaleDateString('fr-FR') %>
                                        </small>
                                        <a href="/actualites/<%= article.slug %>" class="btn btn-sm btn-outline-primary">Lire plus</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="col-12">
                        <p class="text-muted text-center">Aucune actualité disponible pour le moment.</p>
                    </div>
                <% } %>
            </div>
            <div class="text-center mt-4">
                <a href="/actualites" class="btn btn-primary">Voir toutes les actualités</a>
            </div>
        </div>
    </section>

    <!-- Prochains événements -->
    <section class="py-5 bg-light">
        <div class="container">
            <div class="row mb-4">
                <div class="col-lg-12">
                    <h2 class="h3 fw-bold mb-3">Prochains événements</h2>
                </div>
            </div>
            <div class="row g-4">
                <% if (events && events.length > 0) { %>
                    <% events.slice(0, 3).forEach(event => { %>
                        <div class="col-md-6 col-lg-4">
                            <div class="card h-100 shadow-sm">
                                <div class="card-body">
                                    <div class="d-flex mb-3">
                                        <div class="date-box bg-primary text-white text-center rounded p-2 me-3">
                                            <div class="fw-bold fs-4"><%= new Date(event.startDate).getDate() %></div>
                                            <div class="small"><%= new Date(event.startDate).toLocaleDateString('fr-FR', { month: 'short' }) %></div>
                                        </div>
                                        <div>
                                            <h5 class="card-title mb-1"><%= event.title %></h5>
                                            <small class="text-muted">
                                                <i class="bi bi-clock me-1"></i>
                                                <%= new Date(event.startDate).toLocaleTimeString('fr-FR', { hour: '2-digit', minute: '2-digit' }) %>
                                            </small>
                                        </div>
                                    </div>
                                    <p class="card-text small"><%= event.description.substring(0, 100) %>...</p>
                                    <% if (event.location && event.location.name) { %>
                                        <p class="small text-muted mb-2">
                                            <i class="bi bi-geo-alt me-1"></i><%= event.location.name %>
                                        </p>
                                    <% } %>
                                    <a href="/evenements/<%= event.slug %>" class="btn btn-sm btn-primary w-100">
                                        Voir détails
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="col-12">
                        <p class="text-muted text-center">Aucun événement à venir pour le moment.</p>
                    </div>
                <% } %>
            </div>
            <div class="text-center mt-4">
                <a href="/evenements" class="btn btn-primary">Voir tous les événements</a>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="py-5 bg-primary text-white">
        <div class="container text-center">
            <h2 class="h3 fw-bold mb-3">Prêt à nous rejoindre ?</h2>
            <p class="lead mb-4">Devenez membre de l'ASC Zone de Tir et participez à nos activités sportives et culturelles !</p>
            <a href="/auth/inscription" class="btn btn-light btn-lg">
                <i class="bi bi-person-plus me-2"></i>Adhérer maintenant
            </a>
        </div>
    </section>

    <%- include('partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>

    <% if (successMessage) { %>
        <div class="toast-container position-fixed top-0 end-0 p-3">
            <div class="toast show" role="alert">
                <div class="toast-header bg-success text-white">
                    <i class="bi bi-check-circle me-2"></i>
                    <strong class="me-auto">Succès</strong>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="toast"></button>
                </div>
                <div class="toast-body"><%= successMessage %></div>
            </div>
        </div>
    <% } %>

    <% if (errorMessage) { %>
        <div class="toast-container position-fixed top-0 end-0 p-3">
            <div class="toast show" role="alert">
                <div class="toast-header bg-danger text-white">
                    <i class="bi bi-exclamation-triangle me-2"></i>
                    <strong class="me-auto">Erreur</strong>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="toast"></button>
                </div>
                <div class="toast-body"><%= errorMessage %></div>
            </div>
        </div>
    <% } %>
</body>
</html>

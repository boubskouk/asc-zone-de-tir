<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - <%= siteName %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/header') %>

    <!-- Hero -->
    <section class="bg-primary text-white py-5">
        <div class="container text-center py-4">
            <h1 class="display-4 fw-bold mb-3">Actualités</h1>
            <p class="lead">Toutes les nouvelles de l'ASC Zone de Tir</p>
        </div>
    </section>

    <!-- Filtres -->
    <section class="py-4 bg-light">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-8">
                    <div class="btn-group" role="group">
                        <a href="/actualites" class="btn <%= category === 'all' ? 'btn-primary' : 'btn-outline-primary' %>">
                            Toutes
                        </a>
                        <a href="/actualites?categorie=sport" class="btn <%= category === 'sport' ? 'btn-primary' : 'btn-outline-primary' %>">
                            Sport
                        </a>
                        <a href="/actualites?categorie=culture" class="btn <%= category === 'culture' ? 'btn-primary' : 'btn-outline-primary' %>">
                            Culture
                        </a>
                        <a href="/actualites?categorie=evenement" class="btn <%= category === 'evenement' ? 'btn-primary' : 'btn-outline-primary' %>">
                            Événements
                        </a>
                        <a href="/actualites?categorie=vie-associative" class="btn <%= category === 'vie-associative' ? 'btn-primary' : 'btn-outline-primary' %>">
                            Vie associative
                        </a>
                    </div>
                </div>
                <div class="col-md-4">
                    <form action="/recherche" method="GET" class="mt-3 mt-md-0">
                        <div class="input-group">
                            <input type="text" class="form-control" name="q" placeholder="Rechercher...">
                            <button class="btn btn-primary" type="submit">
                                <i class="bi bi-search"></i>
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </section>

    <!-- Articles -->
    <section class="py-5">
        <div class="container">
            <% if (articles && articles.length > 0) { %>
                <div class="row g-4">
                    <% articles.forEach(article => { %>
                        <div class="col-md-6 col-lg-4">
                            <div class="card h-100 shadow-sm">
                                <% if (article.featuredImage) { %>
                                    <img src="<%= article.featuredImage %>" class="card-img-top" alt="<%= article.title %>"
                                         onerror="this.src='https://placehold.co/400x250/1e40af/white?text=<%= encodeURIComponent(article.title) %>'">
                                <% } else { %>
                                    <img src="https://placehold.co/400x250/1e40af/white?text=<%= encodeURIComponent(article.title) %>"
                                         class="card-img-top" alt="<%= article.title %>">
                                <% } %>
                                <div class="card-body">
                                    <div class="mb-2">
                                        <span class="badge bg-primary"><%= article.category %></span>
                                        <% if (article.isFeatured) { %>
                                            <span class="badge bg-warning text-dark">
                                                <i class="bi bi-star-fill"></i> À la une
                                            </span>
                                        <% } %>
                                    </div>
                                    <h5 class="card-title">
                                        <a href="/actualites/<%= article.slug %>" class="text-decoration-none text-dark">
                                            <%= article.title %>
                                        </a>
                                    </h5>
                                    <p class="card-text text-muted small"><%= article.excerpt %></p>
                                    <div class="d-flex justify-content-between align-items-center">
                                        <small class="text-muted">
                                            <i class="bi bi-calendar me-1"></i>
                                            <%= new Date(article.publishedAt).toLocaleDateString('fr-FR') %>
                                        </small>
                                        <small class="text-muted">
                                            <i class="bi bi-eye me-1"></i><%= article.views || 0 %>
                                        </small>
                                    </div>
                                </div>
                                <div class="card-footer bg-white border-top-0">
                                    <a href="/actualites/<%= article.slug %>" class="btn btn-sm btn-outline-primary w-100">
                                        Lire la suite <i class="bi bi-arrow-right ms-1"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>

                <!-- Pagination -->
                <% if (totalPages > 1) { %>
                    <nav class="mt-5">
                        <ul class="pagination justify-content-center">
                            <% if (currentPage > 1) { %>
                                <li class="page-item">
                                    <a class="page-link" href="/actualites?page=<%= currentPage - 1 %><%= category !== 'all' ? '&categorie=' + category : '' %>">
                                        <i class="bi bi-chevron-left"></i> Précédent
                                    </a>
                                </li>
                            <% } %>

                            <% for (let i = 1; i <= totalPages; i++) { %>
                                <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                                    <a class="page-link" href="/actualites?page=<%= i %><%= category !== 'all' ? '&categorie=' + category : '' %>">
                                        <%= i %>
                                    </a>
                                </li>
                            <% } %>

                            <% if (currentPage < totalPages) { %>
                                <li class="page-item">
                                    <a class="page-link" href="/actualites?page=<%= currentPage + 1 %><%= category !== 'all' ? '&categorie=' + category : '' %>">
                                        Suivant <i class="bi bi-chevron-right"></i>
                                    </a>
                                </li>
                            <% } %>
                        </ul>
                    </nav>
                <% } %>
            <% } else { %>
                <div class="text-center py-5">
                    <i class="bi bi-newspaper text-muted" style="font-size: 4rem;"></i>
                    <h3 class="mt-3">Aucune actualité disponible</h3>
                    <p class="text-muted">Revenez bientôt pour découvrir nos dernières nouvelles !</p>
                </div>
            <% } %>
        </div>
    </section>

    <%- include('../partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>

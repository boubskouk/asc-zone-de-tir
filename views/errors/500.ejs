<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>500 - Erreur serveur - <%= siteName %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/header') %>

    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-8 text-center">
                <div class="error-page py-5">
                    <i class="bi bi-bug text-danger" style="font-size: 6rem;"></i>
                    <h1 class="display-1 fw-bold text-primary mt-4">500</h1>
                    <h2 class="h3 mb-3">Erreur serveur</h2>
                    <p class="lead text-muted mb-4">
                        <%= message || 'Une erreur interne est survenue. Nos équipes ont été notifiées.' %>
                    </p>

                    <% if (typeof error !== 'undefined' && error.stack) { %>
                        <div class="alert alert-danger text-start mt-4">
                            <strong>Détails de l'erreur (mode développement) :</strong>
                            <pre class="mt-2 mb-0"><code><%= error.stack %></code></pre>
                        </div>
                    <% } %>

                    <div class="d-flex gap-3 justify-content-center mt-4">
                        <a href="/" class="btn btn-primary">
                            <i class="bi bi-house-door me-2"></i>Retour à l'accueil
                        </a>
                        <a href="javascript:location.reload()" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-clockwise me-2"></i>Réessayer
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
